<div class='text-center'>
<h3>Regístrate</h3>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

  <div class="field">
    <%= f.label :email %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
  </div>

  <div class="field mt-3">
    <%= f.label :contraseña %>
    <% if @minimum_password_length %>
    <em>(<%= @minimum_password_length %> characters minimum)</em>
    <% end %><br />
    <%= f.password_field :password, autocomplete: "new-password" %>
  </div>

  <div class="field">
    <%= f.label :password_confirmation %><br />
    <%= f.password_field :password_confirmation, autocomplete: "new-password" %>
  </div>

  <div class="actions">
    <%= f.submit "Listo", class:'btn btn2 text-white btn-sm mt-4 mb-4' %>
  </div>
<% end %>

<%= render "devise/shared/links", class:'btn btn2 text-white btn-sm mb-4' %>
</div>
<hr>
<p class="text-center mt-5 mb-5"><%= link_to 'Inicio', tasks_index_path, class:'btn search my-2 my-sm-0 text-white btn-sm'%></p>
